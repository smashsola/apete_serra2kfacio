{
  "name": "apete-serra",
  "private": true,
  "version": "0.1.0",
  "description": "APETÊ — demonstração para Siará Tech",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "node --check src/worker.js && node --check public/app.js"
  },
  "devDependencies": {
    "wrangler": "^4.141.0"
  }
}
